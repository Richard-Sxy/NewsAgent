VITE_ROUTER_BASE=/
VITE_APP_TITLE=NewsAgent 运营控制台（本地）
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000

# 本地开发替身：仅在 `vite dev` 的代理层注入身份头，模拟企业网关。
# 这些值不会进入生产构建产物；生产环境由真实网关注入。
# 后端要求 X-Tenant-ID / X-User-ID 为 UUID，Data Loop 额外要求角色与共享 Bearer 令牌。
VITE_DEV_TENANT_ID=22222222-2222-4222-8222-222222222222
VITE_DEV_USER_ID=22222222-2222-4222-8222-222222222222
VITE_DEV_DATA_LOOP_ROLES=data-loop:admin
VITE_DEV_GATEWAY_TOKEN=local-dev-token
# 热点控制台独立角色头（与 Data Loop 复用同一个共享网关 Token）
VITE_DEV_HOT_NEWS_ROLES=hot-news:admin
